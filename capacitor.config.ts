import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'morning-supporter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
